# Jogo da Memória**

**Descrição**:
- Projeto simples de um jogo da memória feito com **HTML**, **CSS** e **JavaScript** (Vanilla).
- O jogo usa pares de emojis como cartas; o jogador revela duas cartas por vez para tentar encontrar pares iguais.

**Estrutura do Projeto**:
- `index.html`: página principal que carrega o jogo.
- `src/scripts/engine.js`: lógica do jogo (embaralhar, abrir cartas, verificar pares, mostrar alerta de vitória).
- `src/styles/main.css`: estilos visuais do jogo.
- `src/styles/reset.css`: reset básico de CSS.
- `README.md`: (este arquivo) documentação rápida.

**Como Jogar**:
- Clique em uma carta para revelá-la.
- Clique em outra carta para tentar formar um par.
- Se as duas cartas forem iguais, elas permanecem visíveis (class `boxMatched`).
- Se não forem iguais, elas viram novamente.
- Use o botão `Reset Game` para recarregar a página e reiniciar o jogo.

Bom jogo! 🎮🧠
